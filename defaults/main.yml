# defaults file for autofs
---
# Below an example which should be placed in inventories variables
# autofs_maps:
# - name: 'misc'
#   options: '--ghost --timeout=600'

# default options for nfs
autofs_nfs_options: '-bg,intr,soft'

# autofs_mounts example
# autofs_mounts:
#   - { mountname: 'backup', options: '{{ autofs_nfs_options }},rsize=1048576,wsize=1048576,nfsvers=3', filer: 'nas0', vol: '/srv/backup' }
#   - mountname: 'pics'
#     options: '{{ autofs_nfs_options }},rsize=1048576,wsize=1048576,nfsvers=3'
#     filer: 'nas1'
#     vol: '/srv/pics'
...
